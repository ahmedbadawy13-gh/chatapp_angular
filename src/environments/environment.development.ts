export const environment = {
  production: false,

  // Same as above, but convenient for local dev
  apiUrl: 'https://YOUR_GPT_ENDPOINT_URL',

  // DO NOT COMMIT REAL SECRETS. Keep this empty or use a local env file approach.
  apiKey: '',

  modelName: 'gpt-4o-mini',
};